[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    lento: pruebas que ejecutan el pipeline completo (decenas de segundos)
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
